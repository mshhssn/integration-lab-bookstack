apiVersion: v1
kind: Service
metadata:
  name: bookstack-app
  namespace: bookstack-lab
  labels:
    app.kubernetes.io/name: bookstack
    app.kubernetes.io/component: app
    app.kubernetes.io/part-of: bookstack-lab
    app.kubernetes.io/managed-by: manual-lab
    app.kubernetes.io/environment: lab
spec:
  selector:
    app.kubernetes.io/name: bookstack
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: 80